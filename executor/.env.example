# Required. Use a long, randomly generated value shared only with the main backend.
EXECUTOR_TOKEN=replace-with-a-long-random-secret

# Optional. Must name an image available to the local Docker daemon.
SANDBOX_IMAGE=codesimul-runner:latest

# Optional. Defaults to 5002.
PORT=5002
