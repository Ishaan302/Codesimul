{
  "name": "codesimul-executor",
  "version": "1.0.0",
  "private": true,
  "description": "Authenticated Docker execution service for CodeSimul.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "check": "node --check index.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "express-rate-limit": "^8.7.0",
    "helmet": "^8.3.0"
  }
}
